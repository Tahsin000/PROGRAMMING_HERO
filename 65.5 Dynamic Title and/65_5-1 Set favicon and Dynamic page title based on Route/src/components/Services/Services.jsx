import React from 'react';
import useTitle from '../../hooks/hooks';

const Services = () => {
    useTitle('Services');
    return (
        <div>
            This is Services Page
        </div>
    );
};

export default Services;