import React from 'react';
import useTitle from '../../hooks/hooks';

const About = () => {
    useTitle('About');
    return (
        <div>
            This is About Page
        </div>
    );
};

export default About;