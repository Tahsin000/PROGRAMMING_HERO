import React from 'react';
import useTitle from '../../hooks/hooks';

const Login = () => {
    useTitle('Login');
    return (
        <div>
            This is Login Page
        </div>
    );
};

export default Login;