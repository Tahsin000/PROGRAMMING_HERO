import React from 'react';
import useTitle from '../../hooks/hooks';

const Register = () => {
    useTitle('Register');
    return (
        <div>
            This is Register Page
        </div>
    );
};

export default Register;